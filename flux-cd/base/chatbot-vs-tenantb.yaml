apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: chatbot
  namespace: tenantb-ns
spec:
  hosts:
  - tenantb.example.com
  gateways:
  # Mention ns as gateway lives in a different ns - otherwise, will not be found by vs
  - multi-tenant-gateway-ns/multi-tenant-gateway
  http:
  - route:
    - destination:
        host: chatbot.tenantb-ns.svc.cluster.local
        port:
          number: 80
