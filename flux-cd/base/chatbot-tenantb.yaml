---
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam:::role/tenantb-app-access-role
  name: tenantb-sa
  namespace: tenantb-ns
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot
  labels:
    app: chatbot
  namespace: tenantb-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatbot
  template:
    metadata:
      labels:
        workload-tier: frontend
        app: chatbot
    spec:
      serviceAccountName: tenantb-sa
      containers:
        - image: 
          imagePullPolicy: Always
          name: chatbot
          ports:
            - containerPort: 8501
          env:
          # - name: ISSUER_URI    not used in app code!
          #   value: https://cognito-idp.ca-central-1.amazonaws.com/ca-central-1_102HyHTxi
          - name: SESSIONS_TABLE
            value: Sessions_tenantb_
        - image: 
          imagePullPolicy: Always
          name: ragapi
          ports:
            - containerPort: 8000
          env:
          - name: CONTEXTUAL_DATA_BUCKET
            value: contextual-data-tenantb-
          - name: CHATHISTORY_TABLE
            value: ChatHistory_tenantb_
          - name: TEXT2TEXT_MODEL_ID
            value: 
          - name: EMBEDDING_MODEL_ID
            value: 
          - name: BEDROCK_SERVICE
            value: 
          - name: AWS_DEFAULT_REGION
            value: ca-central-1
---
kind: Service
apiVersion: v1
metadata:
  name: chatbot
  labels:
    app: chatbot
  namespace: tenantb-ns
spec:
  selector:
    app: chatbot
  ports:
    - port: 80
      name: http
      targetPort: 8501
